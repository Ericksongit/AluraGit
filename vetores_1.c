#include <stdio.h>
#include <stdlib.h>

	int main (void) {

		int i, V[10], soma = 0;

		printf("Digite 10 valores\n");

		for (i = 0; i < 10; i++) {
		scanf("%d", &V[i]);
		
		}
		
		if (V[i] == 30) {
			printf("O numero 30 esta na posição %d \n" i);
		}
		system("pause");
		return 0;
	}